# Task Close Checklist

- Requirement implemented with minimal scope
- Tests executed (`npm test`)
- Build executed when needed (`npm run build`)
- User-facing behavior docs updated (`README.md` when applicable)
- Agent-process docs updated (`AGENTS.md`, `CLAUDE.md`, `.cursorrules` when applicable)
- `MEMORY.md` appended with one new entry
- Final summary includes changed files and verification results
